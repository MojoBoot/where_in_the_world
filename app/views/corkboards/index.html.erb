<div class="col-md-12">
  <div class="table-responsive">
    <h1 style="color:#fff;"> Recent Corkboards </h1>
    <table class="table table-hover">
      <!-- This is a table row with headers -->
      <tr>
        <th> </th>
        <th> </th>
        <th> </th>
      </tr>

      <%@corkboards.each do |corkboard| %>
      <tr>

        <td> <%=corkboard.title %> </td>
        <td> <%=corkboard.comment %> </td>
          <!-- <%=link_to 'Show', location_path(location), class: 'btn btn-default btn-xs' %> -->
          <!-- <%= link_to 'Edit', edit_location_path(location), class: 'btn btn-success btn-xs' %> -->
          <!-- <%= link_to 'Delete', location, method: :delete, data: {confirm: "Confirm delete?"}, class: 'btn btn-xs btn-danger' %>
        </td> -->
      </tr>
      <% end %>
    </table>
  </div>

<div class="well">
  <%= link_to "Add New Corkboard", new_corkboard_path, class: 'btn btn-lg btn-default'%>
</div>

</div>
